# Inventario de componentes — Centro Académico

Índice de los componentes que existen en `src/components/`. Lo consulta el comando `/disenar` antes de codear para **reusar antes de crear**.

> **Mantenimiento:** todo componente nuevo creado con `/disenar` (o a mano) debe agregarse acá. Si un componente cambia de propósito o props clave, actualizar su fila. Este archivo es índice: la fuente de verdad es el código.

## Compartidos (`src/components/ui/`)

Usar SIEMPRE estos antes de crear un equivalente propio del módulo.

| Componente | Para qué | Props clave / notas |
|---|---|---|
| `Icon` | Ícono del sistema (**Material Symbols Outlined**) | `name` (ligadura snake_case, ej. `"check_circle"`), `size?` (px, default 20), `filled?` (FILL 1). Decorativo por defecto (`aria-hidden`). **Única** vía de iconos: no usar otra librería |
| `Button` | Botón del sistema | `variant`: primary (CTA — `secondary` = Azul Conexión, hover `primary`) / secondary (Azul Nexo) / outline / ghost / destructive (`error`) · `size`: sm/md/lg/icon. Tokens de la paleta Nexo Académico, `rounded-sm` = 4px, focus ring `secondary` |
| `Input` | Input de texto estándar | extiende `InputHTMLAttributes`, estilos de foco/token ya resueltos |
| `Textarea` | Área de texto que crece hacia abajo con el contenido (hasta 192px, luego scroll) | extiende `TextareaHTMLAttributes` + `label`/`error`/`hint`. Para textos largos (ej. Dirección, notas) |
| `Select` | Select nativo estándar | extiende `SelectHTMLAttributes` |
| `Combobox` | Select con búsqueda (catálogos largos) | `options: {value,label,tone?}[]`, `sanitize?` (filtro de caracteres al tipear), integra `label`/`error`/`hint` |
| `Modal` | Modal base con overlay + cierre | `open`, `onClose`, `title`, `icon?`, `footer?`, `maxWidth?`; anima con Framer Motion y respeta `prefers-reduced-motion`. HU-MAT-01: el `AnimatePresence` va envuelto en un div `pointer-events-none` mientras está cerrado — si no, el nodo que se está desvaneciendo sigue tapando la pantalla (ver `docs/errores-comunes.md`) |
| `StatusBadge` | Chip de estado (pill + indicador + texto) | `variant`: success/warning/danger/info/neutral/pink + `label`, `icon?` (**nombre de Material Symbols**, ej. `"check_circle"`). Único punto de verdad de colores de estado (tokens `status-*`) |
| `Pagination` | Paginación de tablas | `page`, `totalPages`, `totalItems`, `pageStart/End`, `pageSize`, `onPageChange`, `onPageSizeChange`; HU-ALU-01 agregó `pageSizes?: number[]` (default `[10, 25, 50]` — Alumnos pasa `[20, 50, 100]`) |
| `OrdenamientoSelect` | Orden por fecha | `value`: "recientes"/"antiguas", `onChange` |
| `RangoNumerico` | Filtro numérico min–max | `label`, `valor: {min,max}`, `onChange` |
| `Switch` | Toggle de estado Activo/Inactivo | `role="switch"` + `aria-checked`, `checked`, `onChange`, `ariaLabel`; touch target ≥ 44px |
| `Toast` | Notificaciones de éxito/error | `ToastProvider` + `useToast()` → `showToast("success"\|"error", msg)`. Éxito = verde (`status-success-strong`), error = rojo (`error`) |
| `ConfirmarDialog` | Diálogo de confirmación sobre `Modal maxWidth="max-w-md"` | `open`, `title`, `description`, `confirmLabel`, `cancelLabel="Volver"`, `onClose`, `onConfirm`; `tone?: "danger" (default) \| "success" \| "neutral"` (mapea a Button variant destructive/primary/secondary + icono; `success`/`neutral` para acciones no destructivas). HU-PRO-01 agregó `children?` (contenido opcional extra bajo la descripción, ej: aviso de turnos futuros) |

## Por módulo (`src/components/<módulo>/`)

Patrones recurrentes por pantalla: `<Entidad>Table`, `<Entidad>Filtros*`, `<Entidad>FormModal`, `Estado*Badge`, `Cancelar*Modal`, `<Entidad>Tabs`.

### layout
`Sidebar` (nav principal del Centro Académico): Dashboard · Sedes · Turnos y Agenda · **Alumnos** (`/alumnos`) · **Cuerpo Docente** (`/profesores`) · **Materias** (`/materias`) — los tres ítems activos — · Cobranzas · Usuarios · Reportes. Los módulos no construidos navegan con `href="#"`, quedan `aria-disabled` y muestran el chip "Próx.". Íconos Material Symbols vía `ui/Icon` (`dashboard`, `location_city`, `calendar_month`, `group`, `groups`, `receipt_long`, `school`, `settings`).

### profesores
Módulo HU-PRO-01 (Cuerpo Docente, `/profesores`): `ProfesoresTable` (tabla rediseñada: columnas DOCENTE [avatar iniciales + nombre bold + email] / ESPECIALIDAD-TÍTULO / CONTACTO [teléfono `11-XXXX-XXXX` + email] / MATERIAS [máx 2 + badge `+N`] / CARGA [h sem desde `bloquesPorDia`] / ESTADO / ACCIONES Ver-Editar-Agenda-Baja; orden fijo Apellido+Nombre A-Z, sin orden clickeable; se renderiza dentro de la card que arma `page.tsx` con `ui/Pagination`) · `FiltrosProfesores` (búsqueda placeholder "Buscar..." + Select Materia [Todas las materias] + Select Día [Cualquier día] + botón ghost ⚙ `settings` que despliega panel interno con Select Estado y "Borrar filtros"; contador aria-live; helpers `FiltrosProfesoresState`; el panel lo maneja el propio componente) · `ProfesorFormModal` (formulario 2 modos INSERCION/EDICION — el modo LECTURA se eliminó, lo reemplaza `ProfesorFichaModal`: chips "MODO INSERCIÓN/EDICIÓN" + estado; Combobox "Usuario Asociado del Sistema" solo en inserción (en edición tarjeta bloqueada con `lock`); Título ≤100 opcional; Teléfono 10-11 dígitos; Select Capacidad Máxima por clase (1-10, seed de las capacidades por materia); Materias ≥1 con capacidad 1-10; fieldset **Disponibilidad Horaria Semanal** con franjas (día + De/A cada 30 min, validación fin>inicio y sin superposición, mínimo 1, total "X.X h semanales"); Switch estado; en edición botón destructivo "Dar de Baja Profesor..." → `onBaja` abre el `BajaProfesorModal` de la página) · `ProfesorFichaModal` (**nuevo**, modo LECTURA: `Modal max-w-3xl` "Ficha del Profesor", avatar grande + nombre + título + "Ficha #id · Alta dd/mm/yyyy" — sin legajo; contacto 2 cols; Cap. por Clase; materias en chips; stats Carga Horaria/Turnos/Presentismo; disponibilidad por día; footer Cerrar / Imprimir Ficha / Ir a Editar Profesor) · `AgendaSemanalModal` (**nuevo**, lectura: `Modal max-w-4xl` "Disponibilidad Horaria y Agenda Semanal", nav semana demo + Select Material, leyenda, card "Ocupación semanal %", grilla Lun-Sáb × franjas 1.5 h con celdas `role="gridcell"` (turno / Libre / No disponible); footer Cerrar / Imprimir Agenda / Modificar Bloques → abre `BloquesDisponibilidadModal`) · `BloquesDisponibilidadModal` (editor de bloques estilo form: día + De/A cada 30 min con duración y 🗑, edita SOLO los bloques existentes — el alta se hace desde el form; valida fin>inicio y sin superposición vía `validarFranjas` compartida; total "X.X h semanales" en vivo; expone `Record<number, string[]>` bloquesPorDia) · `BajaProfesorModal` (reescrito: `Modal max-w-lg` "Confirmar Baja de Profesor" — ya no usa `ConfirmarDialog`; caja "Condiciones de la operación" con turnos futuros (**bloqueado** con botón disabled si hay Reservados), Select **Motivo** obligatorio + Textarea observaciones de auditoría; confirma con `(motivo, observaciones)`) · `EstadoProfesorBadge` (mapea sobre `StatusBadge`: activo=success `check_circle`, inactivo=neutral `cancel`)
> Helpers compartidos en **`src/lib/profesores.ts`** (nuevo): franjas ↔ `bloquesPorDia`, `cargaHorariaSemanal`, `formatearTelefono`, `formatearFecha`, iniciales + tonos de avatar (bg /20 del tema + texto fuerte), `HORARIOS_OPCIONES` (08:00-19:30 cada 30 min).
> Datos placeholder en `src/data/profesores.ts` (camelCase; `id` numérico PK real; `// BACKEND:` en usuario-asociado, teléfono, materias, disponibilidad, baja, filtros, turnos). No existe legajo de profesor (solo alumno). Disponibilidad guardada como `bloquesPorDia: Record<diaIso, "HH:MM-HH:MM"[]>` (que es la conversión de las franjas del form). Datos nuevos: tipo `Profesor` con `turnosProgramados?`, `presentismo?` y `capacidadDefault?` (el front usa `profesor_materia.capacidad_maxima`; el CHECK global no tiene columna en el dump) · `TURNOS_SEMANA` (turnos demo de la semana para la grilla de `AgendaSemanalModal`) · `SEMANA_DEMO` (fechas y horas disponibles de la semana demo).
> `layout/Sidebar` y `globals.css` se migraron de Huellitas → **paleta Nexo Académico + tokens del MASTER.md** en HU-PRO-01 (home `/` sigue default hasta próxima HU).
> Iconos: se reemplazó `lucide-react` por **Material Symbols Outlined** (`ui/Icon` + fuente en `layout.tsx` + estilos base en `globals.css`) en todo el sistema, según el documento de diseño.

### materias
Módulo HU-MAT-01 (Materias, `/materias`): `MateriasTable` (columnas Código `MAT-000001`/Materia+descripción/Nivel/Duración/Valor `$ 12.500,00`/Estado/Acciones Ver·Editar·Baja; encabezados clickeables para ordenar; `print:hidden` en la columna Acciones) · `FiltrosMaterias` (búsqueda parcial por nombre + Select Nivel/Estado + chip "Borrar filtros" + botones Exportar CSV / PDF; expone `FiltrosMateriasState` y `FILTROS_MATERIAS_INICIALES` con estado=activo por defecto) · `MateriaFormModal` (formulario único paramétrico 3 modos INSERCION/EDICION/LECTURA: Nombre ≤80 con contador y chequeo de duplicado **en el blur**, Select Nivel, Select Duración alimentado por `DURACIONES_CLASE` del contrato, Valor decimal es-AR con aviso "rige solo hacia adelante" — en EDICIÓN muestra valor anterior → nuevo —, Textarea Descripción ≤250 con contador, Switch estado; en LECTURA campos grises, sin Guardar y con bloque de datos del sistema. Exporta el helper `parsearValor`) · `BajaMateriaModal` (sobre `ui/ConfirmarDialog`, `tone="danger"`; **bloqueada** si hay turnos futuros Reservado, igual que `MATERIA_CON_TURNOS_FUTUROS` del contrato) · `EstadoMateriaBadge` (sobre `StatusBadge`: activo=success `check_circle` "Activa", inactivo=neutral `cancel` "Inactiva") · `exportar.ts` (helpers `materiasACsv`/`descargarCsv`/`imprimirListado`: CSV con BOM UTF-8 y separador `;` para Excel es-AR, PDF vía `window.print()`; **sin dependencias nuevas**).
> Datos placeholder en `src/data/materias.ts`. El shape NO se redefine: es el `MateriaResponse` de `src/contracts/materia.ts` (contrato de API). Helpers `codigoMateria` (deriva `MAT-000001` del id: no es columna de la base), `formatearValor`, `formatearFecha`, `normalizarNombre` (compara duplicados igual que `lower(btrim(nombre))` de la base — **con** tildes, como Postgres).
> `layout/Sidebar` se extendió con el ítem **Materias** (`/materias`, ícono `menu_book`), navegable como Cuerpo Docente.
> `globals.css` sumó un bloque `@media print` para la exportación a PDF; lo que no va al papel se marca con la utilidad `print:hidden`.

### alumnos
Módulo HU-ALU-01 (Alumnos, `/alumnos`): `AlumnosTable` (columnas exactas del criterio N° de legajo / Apellido y Nombre [avatar iniciales + badge de estado al lado] / DNI / Nivel educativo / Teléfono formateado + acción **Ver**; sin orden clickeable — el orden Apellido+Nombre A-Z lo resuelve la capa de datos; Editar y Baja llegan con HU-ALU-02) · `FiltrosAlumnos` (buscador único por legajo/nombre/apellido/DNI con `hint` de qué acepta + Select Estado; expone `FiltrosAlumnosState` y `FILTROS_ALUMNOS_INICIALES` con estado=activo) · `AlumnoFormModal` (formulario paramétrico de **2 modos** INSERCION/LECTURA: franja de datos del sistema con legajo/fecha de alta/badge — nunca inputs —, fieldset Datos del alumno con contadores ≤50, fieldset **Datos del responsable siempre visible** que alterna el chip OBLIGATORIO/OPCIONAL según la edad calculada, aviso inline de posible duplicado, `errorRemoto` con `accion` opcional — lo usa el DNI duplicado para ofrecer "Ver la ficha" —; en LECTURA todo deshabilitado, sin Guardar, con "Reservar turno" disabled + chip "Próx." hasta HU-TUR-01. **Valida con el schema del contrato** `crearAlumnoBody`, traduciendo solo los mensajes genéricos de zod) · `DuplicadosModal` (sobre `ui/ConfirmarDialog` `tone="neutral"`: lista las coincidencias por nombre+apellido+fecha y confirma con **"Cargar igual"** — es un aviso, no un rechazo) · `EstadoAlumnoBadge` (sobre `StatusBadge`: activo=success `check_circle`, inactivo=neutral `cancel`).
> Datos en `src/data/alumnos.ts` (**fixture**, el back todavía no publicó `/api/alumnos`): 24 alumnos, 3 inactivos; el fixture se comporta como el servidor (asigna `id` y `legajo`, aplica el default `estado=activo`, valida `uq_alumno_dni_activo` y lanza `ApiError` con los códigos del contrato). Helpers de dominio `NIVELES_EDUCATIVOS`, `esMenorDeEdad`, `nombreCompleto`. Ver `docs/capa-de-datos-front.md`.
> **`src/lib/formato.ts` (nuevo):** `formatearTelefono`, `formatearFecha`, `edadEnAnios`, `inicialesDe`, `TONOS_AVATAR`, `tonoAvatarDe`. Salieron de `src/lib/profesores.ts` (que los re-exporta, sin romper a sus consumidores) porque no tienen nada de Cuerpo Docente. De paso se corrigió `formatearTelefono` para los números de **10 dígitos**, que partía mal (`38-7455-6677` → `387-455-6677`).

## Reglas de reuso

1. **Reusar antes de crear:** si existe algo equivalente en `ui/`, se usa. Los equivalentes de módulo (ej: `EstadoProfesorBadge`) se evalúan caso por caso; si sirven, se generalizan hacia `ui/` en lugar de duplicarse.
2. **Extender, no duplicar:** si un componente existente casi cumple, se le agregan props/variantes manteniendo retrocompatibilidad con quienes ya lo usan.
3. **Nuevo solo si no hay equivalente**, justificándolo en el plan (qué falta y por qué no conviene extender).
4. **Los badges de estado van sobre `StatusBadge`:** los `Estado*Badge` de módulo solo mapean su estado de negocio a una variante + label + **nombre de ícono Material Symbols**; nunca definen sus propios colores.
5. **Los íconos van sobre `ui/Icon`** (Material Symbols Outlined). Prohibido importar `lucide-react` u otra librería de iconos.
6. **Verificar contra el código:** este inventario puede quedarse atrás; ante duda, confirmar con una búsqueda real en `src/components/**`.